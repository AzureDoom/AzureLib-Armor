package mod.azure.azurelibarmor.core.object;

/**
 * State enum to define whether an {@link mod.azure.azurelibarmor.core.animation.AnimationController} should continue or stop
 */
public enum PlayState {
	CONTINUE,
	STOP
}
