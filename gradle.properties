# Important Notes:
# fabric.mod.json's modid field cannot be expanded, you must change it manually.
# Every field you add must be added to the root build.gradle expandProps map.

# Gradle
org.gradle.jvmargs         = -Xmx3G
org.gradle.daemon          = false
org.gradle.debug           = false

# Project
version                    = 2.1.0
group                      = mod.azure.azurelibarmor

# Common
minecraft_version          = 1.20.4
archives_base_name         = azurelibarmor-1.20.4

# Forge
neo_version                = 20.4.0-beta
neo_version_range          = [20.4,)
loader_version_range       = [1,)
minecraft_version_range    = [1.20.4, 1.21)
mapping_channel            = official
mapping_version            = 1.20.4

# Fabric
fabric_version             = 0.91.1+1.20.4
fabric_loader_version      = 0.15.0

# Mod options
maven_group                = mod.azure.azurelibarmor
mod_name                   = AzureLib Armor
mod_author                 = AzureDoom
mod_version                = 2.1.0
mod_id                     = azurelibarmor
mod_license                = MIT
mod_description            = Based off Geckolib but now just for my own needs.